# 開発ノート

作業のなかで分かったこと・判断したこと・積み残しを日付ごとに足していく。
確定した仕様は [remotelife_tokyo_spec.md](./remotelife_tokyo_spec.md)、データの構造は [data.md](./data.md)、ライセンス判定は [data_license_check.md](./data_license_check.md)。

---

## 2026-08-12 オープンデータの実査とURL確定

東京都オープンデータカタログのCKAN API（`/api/3/action/package_show`）で全件のライセンス欄とリソースURLを引き、実ファイルにHTTPリクエストを投げて取得できることまで確認した。
その結果を `data/src/datasets.py` に反映し、`make ingest` で22ファイル取得・失敗0。

### 判定の訂正 — 統計年鑑は要許諾ではなかった

当初「統計年鑑は issues ページからのリンク先が `www.toukei.metro.tokyo.lg.jp` でカタログサイト外なので、③（要許諾→ダミーデータ）に該当しうる」と整理していたが、**これは誤り**だった。

統計年鑑はカタログサイトにデータセットとして登録されており（例: `t000003d2000001012` 東京都統計年鑑 令和5年 1 土地・気象）、CKAN上のライセンスは `CC-BY-4.0`。
**判定の基準はリソースの実ファイルがどのドメインに置かれているかではなく、カタログに掲載されているかどうか。** 実体が `toukei.metro.tokyo.lg.jp` や `zaimu.metro.tokyo.lg.jp` にあってもルール①が適用される（D13・D16・D17なども同じ形）。

面積・人口をダミーデータで進める必要はない。

### 差し替えたもの

| | 当初の想定 | 差替後 | 理由 |
|---|---|---|---|
| D18 | 東京都統計年鑑（土地・気象 / 人口） | 東京都の人口（推計） | 統計年鑑は最新が令和5年と古い。人口推計なら地域コード・人口・**面積(km2)**・人口密度が1ファイルに揃い、当初の2本立てが1本で済む |
| D17 | 東京の労働力 統計データ | 令和２年国勢調査による東京都の昼間人口 | 「東京の労働力」は都全体の集計で区市町村別の内訳がなく、昼夜間人口比率を作れない |

どちらも53自治体を全件カバーしていることを確認済み。
D18は月次更新でURLの年月部分が変わるので、更新するときはカタログで最新のデータセットIDを取り直すこと。

### 不採用にしたもの

- **D2 自動車騒音の常時監視結果** — カタログ経由で辿れるのは目黒区（BODIK）など個別自治体の公開分のみで、53自治体分は揃わない。区市町村データ（④）なので規約の個別確認も要る。騒音は D4（交通量）と D20（幹線道路密度）で代理する。
- **D6 エコロジカル・ネットワークマップ** — ライセンスは `CC-BY-4.0` で問題ないが、配布物14件がすべてPDF地図。GISデータも数値表もなく指標化できない。

### 追加したもの

- **D20 緊急輸送道路（建設局）** — 「幹線道路密度＝都道の総延長 ÷ 面積」を出そうとしたが、都のカタログに都道そのものの線データがない。国道・都道の主要路線で構成される緊急輸送道路ネットワークが最も近いので代理データとして使う。CC BY・カタログ掲載で条件面の問題はない。

### D5・D15 の個別条件

どちらもカタログ上のライセンス欄が「その他」で、CC BY 4.0 の一括表示から外れている。
D5 の「利用に当たっての注意事項」PDF を読んだところ、大半は精度に関する注意（概略位置である、原典の精度が施設ごとに異なる、属性の面積とポリゴン面積が一致しない等）で、二次利用そのものは妨げていない。

ただし1点だけ制限がある。

- **「宅地化農地」「市街化調整区域内農地」の2レイヤは使わない。** 国土地理院長の承認を得て作成した東京都地形図（S=1:2,500）を使用しており、引用には著作権法上の手続きが別途生じる。
- 使うのは `01_kouenryokuchi.zip`（公園・緑地等）と `03_jurinchi.zip`（樹林地）。

### D7 のコース数の数え方

コースが1件1リソース（KMLのZIP）としてカタログに登録されている。実体を1本も落とさずに、`package_show` のJSON1本でコース数を自治体別に集計できる。
リソースURLのファイル名先頭6桁がチェックデジット付きの団体コードで、その先頭5桁が自治体コードにあたる。

```
.../walkingmap/131148_Myoshoji_River_Course_(Advanced).zip
                ^^^^^^ → 131148 → 13114 中野区
```

距離まで指標化する場合は各KMLの取得が別途必要。
原著作権者の承諾が得られたコースのみ収録のため、収録は29自治体・264コースにとどまり、残り24自治体は `no_data` になる。

### D8 のURL誤記

カタログに登録されている「優先整備区間（シェープファイル）」のURLはホスト名が `www.kensetsu.metro.tokyo.jg.jp`（`lg` が `jg`）になっており開けない。
`https://www.kensetsu.metro.tokyo.lg.jp/content/000035729.zip` が正しい。datasets.py 側は修正済み。

### 交通量（D3・D4）の自治体割当が未解決

軸1の「交通量」に使う予定の2つは、どちらも自治体コードを持つ列がない。

- **D3（警視庁）**: 集計単位が方面・スクリーンライン・交差点。自治体別に落とすには交差点名からの住所推定が要る。参考扱いにとどめる。
- **D4（センサス）**: 1行1観測地点で、列は路線番号／路線名／地点番号／地点名称と車種別交通量。地点名称（例:「中央自動車道富士吉田線〜中央自動車道」）は区間の記述で住所ではない。

どちらもダウンロード自体はできるので取得対象には残してある。指標化の可否は地点名称からの推定精度しだい。

### ingest の実装で踏んだもの

- **xlsx が zip として展開されてしまう。** `zipfile.is_zipfile()` は xlsx にも True を返す。拡張子が `.zip` のときだけ展開するようにした。
- **zip 内の日本語ファイル名が化ける。** 国内配布の zip は格納名が CP932 のことがあり、`zipfile` はそれを CP437 として読む（`緊急輸送道路.shp` → `ï┘ï}ùAæùô╣ÿH.shp`）。UTF-8フラグが立っていないエントリは読み直してから展開するようにした。

### 大容量ファイルの扱い

次の3件は `make ingest` の既定では取得しない。`--heavy` を渡すか、`--only` でIDを指定したときだけ取得する。

| ファイル | サイズ |
|---|---|
| D5 `03_jurinchi.zip`（樹林地） | 約474MB |
| D15 `R03.zip`（区部） | 約363MB |
| D15 `R04.zip`（多摩・島しょ） | 約309MB |

---

## 積み残し

- [ ] **ODPT（公共交通オープンデータセンター）のユーザ登録** — D12の実データ取得に必要。出社軸の4指標が丸ごとこれに依存しているので優先度が高い。登録後に取得先URLを datasets.py に入れる。
- [ ] **`indicators.py` の追従** — 今回の差替に合わせて次の3点を直す。
  - `road_noise_leq`（D2）を外す
  - `arterial_road_density` の `dataset_id` を D19 → D20
  - `day_night_population_ratio` に対応する `normalize.py` のD17ハンドラで、列指定を昼夜間人口比率の列へ（現状は `pick_column(df, "population")` の仮置き）
- [ ] **交通量の自治体割当** — 地点名称からの住所推定を試す。精度が出なければ軸1は PM2.5 と幹線道路密度の2指標で回す。
- [ ] **D18のURL更新** — 月次でURLが変わる。公開直前に最新月へ差し替える。
